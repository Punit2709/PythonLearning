import pyttsx3
import PyPDF2


# book = open('ProjectsPY/Python_HandBook.pdf', 'rb')
# pdfread = PyPDF2.PdfReader(book)
# # print(pdfread.numPages )

# page = pdfread.getPage(2)
# text = page.extractText()

# speaker = pyttsx3.init()
# speaker.say(text)
# speaker.runAndWait()


# import PyPDF2

# book_path = 'ProjectsPY/audioBook.py'  # Replace with the actual path to your PDF file
# pdfreader = PyPDF2.PdfReader(book_path)

# text = ""
# for page_num in range(len(pdfreader.pages)):
#     page = pdfreader.pages[page_num]
#     text = page.extract_text()
    # Do something with the extracted text, like converting it to speech
    # For example, you can use a text-to-speech library to read out the text.

# speaker = pyttsx3.init()
# speaker.say(text)
# speaker.runAndWait()

# pdfreader.close()  # Close the PdfReader object when done

# self.read()